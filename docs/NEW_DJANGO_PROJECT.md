Creating a new empty django project:

	$ django-admin.py startproject <project name>
	$ cd <project name>
	$ python manage.py migrate
	$ python manage.py runserver localhost:8000

For info on using `PostgreSQL` see `SETUP.md`.